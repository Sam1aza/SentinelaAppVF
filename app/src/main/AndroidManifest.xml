<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.samiaza.sentinela.v3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".list_item"></activity>
        <activity android:name=".MedicamentoEstoque" />
        <activity android:name=".Apontamento_remedio" />
        <activity android:name=".PressaoCardiaca" />
        <activity android:name=".Glicose" />
        <activity android:name=".Apontamento" />
        <activity android:name=".Pag_inicial" />
        <activity android:name=".Cadastrar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>